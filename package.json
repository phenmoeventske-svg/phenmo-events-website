{
  "name": "phenmo-website",
  "version": "1.0.0",
  "private": true,
  "description": "Phenmo Events static website and staff portal",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "sync:supabase": "node server/sync-to-supabase.js",
    "test": "node --check server.js && node --check staff-portal/app.js && node --check staff-portal/sw.js && node test/api.test.js && node test/security.test.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "@supabase/ssr": "^0.12.7",
    "@supabase/supabase-js": "^2.116.0",
    "bcryptjs": "^3.0.0"
  }
}
